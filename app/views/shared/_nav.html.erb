<nav class="navbar nav_bar navbar-inverse  " style="background: white; padding: 0px; border:0px;  " >
 
  
    <div class="collapse navbar-collapse  col-xs-offset-2 col-xs-8 col-xs-offset-2 " style="border-radius:0px">




      <ul class="nav navbar-nav">

        <li>    <a  href="/">Home</a> </li>

        <li class="dropdown">
          <a href="#" data-toggle="dropdown" class="dropdown-toggle">Categories <b class="caret"></b></a>
          <ul class="dropdown-menu">

            <% Category.all.each do |c|%>
            <li>
              <%= link_to "#{c.name}", "/index/Category-#{c.id}-#{c.name}" %>
            </li>
            <% end%>
          </ul>
        </li>






        <li class="dropdown">
          <a href="#" data-toggle="dropdown" class="dropdown-toggle">   Best Sellers <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><a href="#">  arts</a></li>
            <li><a href="#"> artists</a></li>
      

          </ul>
        </li>


        <li class="dropdown">
          <a href="#" data-toggle="dropdown" class="dropdown-toggle">Framed Art <b class="caret"></b></a>
          <ul class="dropdown-menu">

            <% Frame.all.each do |f|%>
            <li>
              <%= link_to "#{f.name}", "/index/Frame-#{f.id}-#{f.name}" %>
            </li>
            <% end%>
          </ul>
        </li>



        <li class="dropdown">
          <a href="#" data-toggle="dropdown" class="dropdown-toggle">Size <b class="caret"></b></a>
          <ul class="dropdown-menu">

            <% Size.all.each do |s|%>
            <li>
              <%= link_to "#{s.size}", "/index/Size-#{s.id}-#{s.size}" %>
            </li>
            <% end%>
          </ul>
        </li>




        <li><a href="#">Artists</a></li>

        <li><a href="/promotion/promotion">Promotions</a></li>

<!--         
 <a type='button' class="btn btn-warning pull-right" style="margin-top:6px; border-radius:0px; min-width: 90px;padding: 5px;border:0px" data-toggle="modal" data-target="#popUpWindow"><span class="glyphicon glyphicon-shopping-cart "></span> Cart item(s) 0  Amount : 0.00 mkw </a>

 -->



      </ul>








      <!-- <ul class="nav navbar-nav navbar-right">
        <li>


          <button type="button" class="btn btn-info " style="margin-top:6px; border-radius:0px;" data-toggle="modal" data-target="#myModal">Sign Up</button></li>

         
          <button type='button' class="btn btn-success" style="margin-top:6px;border-radius:0px; margin-left:2px " data-toggle="modal" data-target="#popUpWindow"><span class="glyphicon glyphicon-log-in"></span> Login </button>

        </ul> -->
      </div><!--/.nav-collapse -->
    </div>
  </nav>


  <%  if flash[:notice] %>
  <div col-xs-offset-4 col-xs-4 col-xs-offset-4>
    <div class="alert alert-success ">
      <button data-dismiss="alert" class="close" type="button">×</button>
      <h4>Success</h4>
      <%= flash[:notice] %>
    </div>
  </div>
  <% end %>

  <%  if flash[:error] %>
  <div col-xs-offset-4 col-xs-4 col-xs-offset-4>
    <div class="alert alert-error col-xs-offset-4 col-xs-4 col-xs-offset-4">
      <button data-dismiss="alert" class="close" type="button">×</button>
      <h4>Error</h4>
      <%= flash[:error] %>
    </div>

  </div>
  <% end %>












  <%
  count = 1
  Category.all.reverse.each do |cat|
   img = cat.path
          # raise img.to_s.inspect
          %>



          <!-- modal part -->


          
          <div id="<%= cat.name %>" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
           <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;  </button>
                <h4 class="modal-title" id="myModalLabel"><%= cat.name %>  category </h4>
              </div>
              <div class="modal-body">
                <div class="container-fluid">
                  <div class="row">

                    <div class="col-xs-12">

                      <div class="col-xs-8">

                        <%= image_tag(img, class: "img-responsive",:style=>"width:90%; height:90px;")%>


                      </div>



                      <div class="col-xs-4">

                       <p class="text-muted" style="margin-top: 20px"> <%= cat.subject %></p>

                     </div>
                   </div>



                 </div>
                 <div class="modal-footer">
                  <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                  <button type="button" onclick="show_by_category('<%= cat.name %>')" class="btn btn-primary">More </button>
                </div>
              </div>
            </div>

          </div>
        </div>
      </div>



      <% 
      count += 1
    end

    %>


    <script type="text/javascript">

      function validateForm() {

    // getting todays date


    // end

    var pass = document.forms["myForm"]["password"].value;


    var c_pass = document.forms["myForm"]["c_password"].value;
    // alert(c_pass)
    if (pass != c_pass){
      alert("Password Mismatch!");
      return false;
    }
  }

  function show_by_category(cat){
        window.location.href = "/arts/index?name=" +cat;
  }

  function show(c)
  {

    // arlet(c)
    if( c == "c")
      document.getElementById('artist').style.display="none" ;
    else{
      document.getElementById('artist').style.display="block" ;
      document.getElementById('image').style.display="block" ;
    }
  }




</script>
