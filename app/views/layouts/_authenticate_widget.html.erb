<% if authenticate_signed_in? %>
  <p>Welcome <%= current_authenticate.email %></p>
 
   <%= link_to('Logout', destroy_authenticate_session_path, method: :delete) %> 
<% else %>
  <p>You are not signed in.</p>
 
   <li><%= link_to "Login", new_user_session_path %></li>
<% end %>
